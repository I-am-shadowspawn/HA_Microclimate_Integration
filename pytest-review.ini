[pytest]
asyncio_mode = auto
testpaths = tests harness_tests
addopts = --tb=short --disable-socket --allow-unix-socket --strict-markers -ra
timeout = 30
timeout_method = thread
markers =
    known_defect: acceptance regression blocked by an open checklist item
xfail_strict = true
