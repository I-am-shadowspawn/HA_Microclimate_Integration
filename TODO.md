# Microclimate V2 — public release TODO

> **Unofficial, independent project.** This integration and its custom cards are not affiliated with, endorsed by, or supported by Microclimate. Their only connection to Microclimate is that they work with its products. Product names are used solely to identify compatibility. For integration support, use this project’s [GitHub Issues](https://github.com/I-am-shadowspawn/HA_Microclimate_Integration/issues).

Updated 26 September 2026. Baseline: 1.4.1 release candidate on the V2-17 branch; 1.4.0 is the published release. This replaces earlier active checklists. See [review](docs/V2-READINESS-REVIEW.md) for findings and [archived TODO](docs/TODO-PRE-V2-HISTORY.md) for history. Checked items are complete; unchecked items remain in the V2 backlog.

Priority: **P0** public-release acceptance; **P1** recommended V2 maintenance (may defer only with an explicit recorded decision); **P2** optional future scope. Owner: **engineering**, **maintainer**, or **hardware tester**. Every implementation checkpoint must record changes, tests and next action; preserve safe state if interrupted.

## Publication and installation — P0

- [x] **V2-01 — Establish the public repository and ownership.** Maintainer + engineering. Confirm GitHub owner/repository, real codeowners and issue/documentation URLs; select a project licence and verify distribution rights for inherited code, dependencies and brand art. Add LICENSE, contributor/support/security guidance and issue templates. Do not invent owner names, grant a licence without owner agreement, or publish credentials/test captures. Completed 25 September 2026: ownership/licence confirmed, bundled attribution verified, support/security/contributor documents and templates added, and source published in the agreed GitHub repository. See docs/V2-PUBLICATION-PREPARATION.md.
- [x] **V2-02 — HACS metadata and brand assets.** Engineering; depends on V2-01. Add root hacs.json with name and tested minimum HA version; complete manifest documentation/issue_tracker/codeowners; check integration_type applicability. Add an approved icon in custom_components/microclimate_integration/brand. Keep the existing domain. Completed 25 September 2026: original draft icon, metadata and workflow published; actual HACS and hassfest GitHub Actions pass with no ignored checks. The icon is the agreed draft for this candidate; any final design change before stable release is a later branding decision. See docs/V2-PUBLICATION-PREPARATION.md. References: [HACS integration](https://hacs.xyz/docs/publish/integration/), [general manifest](https://hacs.xyz/docs/publish/start/).
- [x] **V2-03 — Correct release contents.** Engineering. Update scripts/build_release.py to include required binary assets, licence/notices and future YAML/metadata explicitly, while excluding tokens, caches, venvs and captures not approved for public distribution. Choose normal HACS folder installation; do not enable zip_release without testing its required archive layout separately from the manual config-root ZIP. Completed 25 September 2026: explicit runtime/development inputs and reviewed fixture allowlist; deterministic ZIPs and checksums; 3 archive tests cover content, exclusions, extraction and repeatability; CI verifies committed JS/notices against frontend source. No zip_release. See README-TESTING.md.
- [x] **V2-04 — Portable CI and release pipeline.** Engineering. Add HACS validation, hassfest, Python tests, frontend typecheck/lint/unit/browser tests and reproducible build/artifact checks. Add Python formatting/lint configuration; introduce types for touched interfaces rather than indiscriminately rewriting everything. Replace hard-coded local interpreter/browser/cache paths in contributor instructions and scripts with portable configurable setup. Pin supported toolchains and update dependencies deliberately. Implemented 25 September 2026: pinned Python 3.14.7/uv 0.6.1 and Node 24.21.0 workflow with minimum Core 2026.9.3 test gate, frontend type/lint/unit/browser checks, HACS/hassfest, reproducible package job and gated tagged release. GitHub's 1.3.2 branch/tag validation and release jobs passed. See README-TESTING.md. [HACS action](https://hacs.xyz/docs/publish/action/).
- [ ] **V2-05 — Supported-version policy.** Maintainer + engineering. Decide the minimum HA version based on exercised APIs, test that minimum plus current stable, and add a scheduled upcoming-version compatibility check if practical. Document tested Python/browser/HA combinations and support policy. The decided minimum Core 2026.9.3 is in hacs.json, README and CI; 2026.9.2 remains historical evidence only. Current/future support policy and scheduled compatibility checks remain to be completed under this item. Done: matrix, hacs.json and README agree; no unsupported backwards-compatibility claim.
- [ ] **V2-06 — Card install/update lifecycle.** Engineering. Keep bundled backend/cards together. Define and test resource registration/cache refresh on first install, upgrade, rollback and uninstall, including storage and YAML dashboards. Prefer supported/idempotent automation where available; otherwise give exact one-time resource steps. Guard duplicate custom-element registration and duplicate card-picker entries; report stale bundle/backend mismatches clearly. **Partial evidence, 25 September 2026:** HACS custom-repository prerelease install/update and release 1.3.2 update passed; existing cards worked across upgrade, new cards were created, and season dates, temperatures and schedule times edited successfully. **Still open:** rollback/uninstall, storage and YAML dashboard resource behavior, duplicate registration and stale bundle handling. Those were not established by the reported HACS test, so V2-06 remains unchecked.
- [ ] **V2-07 — Test HACS distribution, then submit catalogue listing.** Maintainer + tester; after V2-01–06. **Custom-repository portion passed 25 September 2026:** prerelease commits installed and updated, and release 1.3.2 updated through HACS. GitHub release 1.3.2 was published. Remaining: catalogue submission and external acceptance; rollback evidence is also tracked in V2-06. [Default inclusion](https://hacs.xyz/docs/publish/include/).

## Public operation and support — P0

- [x] **V2-08 — End temporary diagnostic defaults.** Raw-pin entities now default disabled, and setup no longer changes existing registry choices. Reported schedule periods remains enabled as a normal sensor and card permission anchor; other diagnostics and the enabled-write default remain available. Fresh-install, reload, permission and card-save regressions pass with raw pins disabled. Live HACS installation on 25 September 2026 confirmed disabled defaults for new devices and retention of existing user-enabled diagnostic states. See `harness_tests/test_sensor_platform.py`, `test_compact_exposure.py` and `test_write_runtime.py`.
- [x] **V2-09 — Support diagnostics and privacy.** HA Download diagnostics now exports a bounded, redacted model/version/options/status/response-shape summary, never raw pin names or values. Nested, encoded, malformed and oversized input tests pass; full response logging remains opt-in. Public fixtures and release files were audited; a local workstation path was removed, and package tests reject personal paths and credential fields in public fixtures. See `harness_tests/test_diagnostics_export.py` and `tests/test_release_package.py`.
- [ ] **V2-10 — Freeze supported features and user documentation.** Maintainer + engineering. Publish a model/channel/firmware evidence matrix distinguishing captured, user-confirmed and unverified behavior. Explain cloud/token setup, no-local fallback, permissions, retained controls, unavailable Constant/Periodic writes, non-atomic schedules, recovery and support contact. Remove stale “candidate 1.1.1” instructions from active docs. Explain aggregate-sensor dependency and Celsius values mislabeled F upstream. Done: a new user can install/configure/recover using public docs alone; no workstation paths or contradictory old instructions.
- [ ] **V2-11 — Identity and public upgrade contract.** Engineering. Keep the agreed clean transition from the one disposable prototype; no prototype migration required. Freeze V2 entity/device identity and config schema before stable release. From that public baseline, test normal version upgrades, token reauth, rename and rollback without requiring users to recreate devices. Add migrations only when an actual future schema change requires one. Done: two candidate versions can be upgraded with stable registry/card bindings.
- [ ] **V2-12 — Real-install acceptance.** Tester. Confirm 1.3/V2 compact setup, schedule editing, independent modes/alarms/ramps/root dates, restricted-user behavior, enabled/disabled aggregate sensors, companion/mobile UI and Fahrenheit display after actual reinstall. **Passed HACS coverage 25 September 2026:** new device workflow, diagnostic defaults and preservation of existing enabled diagnostics, card creation and upgrade continuity, season-date, temperature and schedule-time edits. Modes, alarms/ramps, restricted-user, mobile and Fahrenheit checks still need explicit evidence. Done: dated model/firmware/HA/browser results, issues fixed or explicitly scoped out.
- [ ] **V2-13 — Failure/persistence/soak acceptance.** Tester + engineering. Record controlled mode-switch/reboot persistence; midnight and season/year transitions; partial writes, lost acknowledgements, app conflicts, token expiry, disconnected probes and outages. Run 24–48 hours with request rate, recovery, memory, logs and recorder growth recorded. Do not infer durability from API readback. Done: no unexplained replay, runaway polling, orphan jobs or secret logs; known remote limitations documented. Never run physical writes unattended without authorization for those experiments.

## Focused implementation cleanup — P1

- [x] **V2-14 — Remove remaining test-only transformation scaffolding.** Engineering. Audit transformation.py/const_helpers.py consumers. Keep convert_temperature (validation.py actively imports it), enum normalization and live canonical maps. Retire unconsumed formatting/time/enum dispatch and recursive pin wrappers if no runtime consumer exists; port meaningful tests to the current schedule/read/write contracts. Done: import audit clean, canonical pin equality and upstream-F-labelled-Celsius regressions preserved; no duplicate parser behavior hidden behind legacy tests.
- [x] **V2-15 — Simplify the base entity and observational climate.** Engineering. Replaced the obsolete name-based base identity with entry/channel device information. The climate keeps its unique ID and naming pattern while following entry and reported channel renames. Removed unused entity_name and duplicate refresh/state writes; Home Assistant's CoordinatorEntity now owns refresh/publication. Climate setter errors identify that climate is read-only and direct users to separate configuration controls. Focused tests cover naming, IDs, read-only setters, one-refresh publication, unavailable/recovery and unload; the Core 2026.9.3 release gate passed 662 tests at 96% coverage.
- [ ] **V2-16 — Typed runtime and job ownership.** Engineering. Make coordinator/card runtime fields explicit and typed; separate WebSocket handlers from batch job lifecycle/planning. Evaluate entry.runtime_data and supported coordinator hooks to reduce private _async_refresh/_card_* coupling. Keep a single shared lock/I/O owner. Done: permission revocation, cancellation, duplicate request IDs, no-op jobs, unload/reload and cross-entry isolation retain regression coverage. No generic framework or new poller.
- [x] **V2-17 — Consolidate parsing and validation contracts.** Engineering. Document observation vs edit types, field applicability and constraints once; reuse compatible helpers in projection and writes. Add shared fixture vectors for Python and TypeScript rather than relying on matching hard-coded limits. Preserve read-side unset/unknown values and strict write-side dates/ranges; do not erase opaque time data. Completed 26 September 2026: [contract and ownership audit](docs/VALIDATION-CONTRACT.md), shared synthetic fixture vectors executed by Python and TypeScript, and reused numeric/time/schedule helpers. Observation and strict edit semantics remain separate; card validation now rejects unset date writes and invalid Day/Night/Seasonal cardinality before Save, matching the backend. Enum families, non-finite inputs, Celsius/% modes, 0/100, 0/240, date/time boundaries, applicability and opaque time suffixes are covered. Validation: 753 backend tests (96% coverage), 115 frontend unit tests, 44 browser tests, type/lint checks and reproducible release archives passed. Version 1.4.1 awaits PR merge and release tagging.
- [ ] **V2-18 — Split card presentation and state management.** Engineering. Extract schedule row/timeline/settings and Save/subscription lifecycle from the large card.ts. Keep draft state separate from live snapshots. Add typed runtime validation for malformed/missing WebSocket fields, accessible keyboard/touch control checks, timezone/units labels and user-facing localized messages. Done: all existing browser scenarios, refresh-while-typing, HTTP UUID handling, table collapse, colours and partial-save recovery remain passing; add Firefox/WebKit or real companion checks where supported.
- [ ] **V2-19 — Measure read amplification and notification load.** Engineering. Instrument mocked representative 2/8-point saves and idle polls; establish read/write/state-event counts. Current successful N-step batches normally use 3N+2 reads plus N updates. Consider sharing the final pre-dispatch baseline between orchestration and the write primitive, retaining permission/context checks immediately before dispatch, per-pin readback and final confirmation. Done: measured savings plus concurrency/failure tests; defer if benefits do not justify risk. No parallel writes, blanket debounce or automatic retry.
- [ ] **V2-20 — Clean production/test layout and names.** Engineering. Standardize relative imports, formatting, annotations and descriptive modules; rename test_schedule_field_entities.py for its aggregate-projection role. Consolidate fixtures under a normal tests layout; avoid cross-importing fixtures from test modules. Keep captured provenance and release evidence outside shipped runtime. Done: contributor commands and CI remain straightforward; no tests exist solely to preserve removed compatibility code.
- [ ] **V2-21 — Improve actionable errors and resource limits.** Engineering. Preserve sanitized error classes across read/setup layers instead of collapsing every failure to a generic message; distinguish unavailable, invalid payload, throttled and auth without leaking URLs. Audit write-job/history limits and timeouts at maximum slot count, slow readback and many entries. Done: user messages explain the next safe action, no unbounded history/subscriptions, no regression to automatic retries or speculative rollback.

## Optional features / evidence-dependent backlog — P2, not V2 blockers

- [ ] **V2-22 — Confirm unknown vendor fields.** Hardware tester. Resolve v24 units, v25 semantics and Blue periodic interval/duration units/ranges/phase only from controller evidence. Preserve raw unknowns until then. This does not reopen the already-confirmed family timing maps.
- [ ] **V2-23 — Constant target and Periodic editing.** Engineering after V2-22/verified mapping. Never use observed setpoint pins or slot 1 as guessed Constant controls. Deliver model-specific tests and exposure only after evidence; unsupported modes remain documented in V2.
- [ ] **V2-24 — Calculated current-period display.** Define timezone, DST, boundary, midnight and season-transition rules; expose one clearly calculated sensor with period/start/end/expected-target attributes. Never claim device-confirmed active period from the stored schedule alone.
- [x] **V2-25 — Presets/copy/import/export and automation actions.** Reuse the validated planner for optional templates or a documented whole-schedule action. Define device compatibility, units, permissions and partial-failure behavior. Keep this separate from direct per-device storage; do not reintroduce 32 entities per channel.

## Execution order and release decision

1. Resolve maintainer decisions in V2-01 and minimum support policy; prepare repository/CI/packaging and public defaults.
2. Implement V2-14–18/20 in small independently tested changes; assess V2-19/21 and record any deliberate deferral.
3. Complete documentation, redacted support tools and public upgrade contract; run the full test matrix on the chosen supported versions.
4. Exercise HACS custom-repository install/update and targeted hardware/soak acceptance. Fix regressions; build a stable 2.0.0 release only when all P0 technical/test requirements pass. Default-catalogue acceptance remains external to software completion.
5. Publish and submit only when explicitly instructed; maintain the single active TODO with concrete evidence links. Do not alter prior release ZIPs or apply changes to live controllers during routine implementation.

Confirmed publication inputs (25 September 2026): owner/codeowner `I-am-shadowspawn`; repository `https://github.com/I-am-shadowspawn/HA_Microclimate_Integration`; project licence MIT. Manifest codeowner, integration_type=device, README documentation and GitHub Issues URLs are set. Root LICENSE contains the MIT licence, copyright (c) 2026 Simon Burke, as confirmed by the maintainer. Initial Core minimum set to 2026.9.3 in root hacs.json. Maintainer-confirmed full-test platform: Core 2026.9.3, Supervisor 2026.09.2, OS 18.2, Frontend 20260826.7 (recorded in docs/TESTED-PLATFORM.md). V2-01 and V2-02 are complete: public source and ownership/support metadata are present; HACS and hassfest validations passed. HACS custom-repository install/update and published release 1.3.2 passed maintainer validation; V2-06 remains open for lifecycle cases not exercised and V2-07 remains open for catalogue acceptance. See docs/V2-PUBLICATION-PREPARATION.md.

Publication branding status (25 September 2026): the original generated thermometer/clock draft replaces the supplied gecko as the candidate icon. The gecko remains outside the source tree and is excluded from all packages. No vendor affiliation or permission is implied. See docs/BRANDING.md for provenance. V2-02 is complete after public HACS and hassfest validation; a later icon design change does not reopen this check.
